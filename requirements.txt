Flask==3.0.3
openpyxl==3.1.5
docxtpl==0.18.0
python-docx==1.1.2
Unidecode==1.3.8
pyinstaller==6.21.0
zeep
# zeep still imports the stdlib `cgi` module, removed in Python 3.13 - this
# shim restores it. Harmless/unneeded (marker keeps it uninstalled) on older
# interpreters.
legacy-cgi; python_version >= "3.13"
