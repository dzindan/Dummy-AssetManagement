"""Bumped by hand on every build so the running app can prove which build it
is, in its own footer - useful when a rebuild doesn't seem to "take" and it's
unclear whether that's a stale process, a stale browser tab, or a stale
PyInstaller build cache (always `rm -rf build dist` before rebuilding)."""

APP_VERSION = "2026-08-24.1"
